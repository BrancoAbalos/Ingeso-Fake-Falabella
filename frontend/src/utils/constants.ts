
// Resource URLs
export const RESOURCES = Object.freeze({
  VITE_LOGO: '/vite.svg',
  VITE_BACK: '/vite.svg',
} as const);

// Route Constants
export const ROUTES = Object.freeze({
  LOGIN: '/login',
  REGISTER: '/register',
  SETTINGS: '/settings',
  PAYMENT: '/payment',
  CHECKOUT: '/checkout',

  HOME: '/',
} as const);